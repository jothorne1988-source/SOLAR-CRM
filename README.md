# 🌞 Solar CRM MVP

This is a **React-based Solar Sales CRM** prototype with:

- Kanban workflow (Lead → Survey → Proposal → Install → Commissioned)
- Google Maps array designer
- AI assistant (lead scoring, roof area estimates, proposal drafting)
- WhatsApp-style integrated chat UI
- Role-based views (Admin, Sales, Surveyor, Installer)

## 🚀 Getting Started

1. Clone the repository
2. Install dependencies:
   ```bash
   npm install
   ```
3. Run the app:
   ```bash
   npm run dev
   ```

## 🔑 API Keys
Add your **Google Maps API Key** in `App.tsx` (replace `YOUR_GOOGLE_MAPS_API_KEY_HERE`).

---
Made with ❤️ by Jo Thorne
